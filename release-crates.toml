[release]
version = "0.1.0"
policy = "independent"

[crates."unitedstates-core"]
previous_version = "0.0.0"
version = "0.1.0"
change = "code"
publish = true
reason = "Initial dependency-free shared contracts."

[crates.unitedstates]
previous_version = "0.0.0"
version = "0.1.0"
change = "code"
publish = true
reason = "Initial dependency-free facade over unitedstates-core."
